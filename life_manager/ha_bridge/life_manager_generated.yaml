# AUTO-GENERATED BY LIFE MANAGER.
# Do not edit this file manually; it may be replaced on add-on startup.
#
# This bridge intentionally contains only stable write actions used by
# Life Manager frontend cards. Read data continues through the existing
# sensor.life_manager REST sensor.

rest_command:
  life_manager_bridge_occurrence:
    url: "http://127.0.0.1:8000/quests/{{ quest_id }}/occurrence"
    method: POST
    headers:
      x-api-key: !secret life_manager_api_key
    content_type: "application/json"
    payload: >-
      {{
        {
          "action": action,
          "target_date": target_date | default(none),
          "note": note | default(none)
        } | to_json
      }}

script:
  life_quest_occurrence:
    alias: Life Manager - Quest verschieben/auslassen
    mode: queued
    fields:
      quest_id:
        required: true
      action:
        required: true
      target_date:
      note:
    sequence:
      - action: rest_command.life_manager_bridge_occurrence
        data:
          quest_id: "{{ quest_id }}"
          action: "{{ action }}"
          target_date: "{{ target_date | default(none) }}"
          note: "{{ note | default(none) }}"
